============
Installation
============

At the command line::

    $ pip install broadview-lib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv broadview-lib
    $ pip install broadview-lib